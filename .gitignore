# --- PriorMail ML: never commit these (see CLAUDE.md §11) ---
# Datasets (raw, processed, labeled) — privacy + reproducibility
/data/
# Raw Enron corpus at repo root (loader default path) — never commit raw data
/emails.csv
# Trained checkpoints — published to Supabase Storage, not git
/checkpoints/
# Experiment tracking artifacts
/wandb/
# Eval outputs (regenerated by the harness)
/eval/results/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# --- Virtual environments / uv ---
.venv/
venv/
.env
.python-version

# --- Tooling caches ---
.mypy_cache/
.ruff_cache/
.pytest_cache/
.ipynb_checkpoints/
htmlcov/
.coverage
.coverage.*

# --- Editors / OS ---
.vscode/
.idea/
.DS_Store

# --- Secrets ---
# Supabase keys, wandb tokens, etc. live in env, never in git
*.local
secrets.toml
